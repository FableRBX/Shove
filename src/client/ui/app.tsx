import React from "@rbxts/react";

/**
 * Spine shell, mounted by UiController inside the PushAGiantApp ScreenGui:
 * fork-wide providers and always-on UI go here. Empty in the template.
 */
export function App() {
	return <></>;
}
